# Copy this file to android/keystore.properties and fill in real values.
# android/keystore.properties is gitignored -- never commit real
# passwords or the keystore file itself.
#
# storeFile is relative to the android/ directory.
storeFile=app/release.keystore
storePassword=CHANGE_ME
keyAlias=chordbook
keyPassword=CHANGE_ME
